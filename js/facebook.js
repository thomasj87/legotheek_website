// Placeholder: automatisch posten naar een Facebook-pagina.
// Nog niet actief — vereist een Facebook-app met paginatokken (zie README.md).
async function autoPostNaarFacebook(post) {
  if (!CONFIG.facebookPagina) return;
  console.log("TODO: post naar Facebook:", post.titel);
}
